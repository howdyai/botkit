// every bot needs a "random" topic as an entry point and fallback
! local concat = newline


> topic random inherits global_commands

    + start
    - welcome to ribot Rivescript demo
    ^ I have some things like `asl` or building a `menu`
    ^ also I can display `media`.

    + gambit
    - {@demo}

    // - {topic=asl}{@start}

    + *
    - <star>? {@gambit}

< topic
